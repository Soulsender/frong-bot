services:
  frong-bot:
    image: soulsender/frong-bot:2.2
    environment:
      - TOKEN=your_token_here
      - OPENAI_KEY=your_api_key_here
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    
